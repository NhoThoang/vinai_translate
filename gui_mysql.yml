git reset folder_name
git rm -r --cached folder_name

SHOW GRANTS FOR 'username'@'host';
SELECT user, host FROM mysql.user;
create user
CREATE USER 'username'@'host' IDENTIFIED BY 'password';

GRANT ALL PRIVILEGES ON database_name.* TO 'newuser'@'host';
FLUSH PRIVILEGES;
only insert update delete select
GRANT SELECT, INSERT, UPDATE, DELETE ON database_name.* TO 'newuser'@'host';
show permissions for 'username'@'host';
SHOW GRANTS FOR 'newuser'@'host';
deler user
DROP USER 'newuser'@'host';
recall permission
REVOKE quyền, SELECT ON database.* FROM 'username'@'host';
REVOKE ALL PRIVILEGES ON database_name.* FROM 'username'@'host';

